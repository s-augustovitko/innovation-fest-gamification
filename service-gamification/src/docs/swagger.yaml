definitions:
  models.Badge:
    properties:
      name:
        type: string
    type: object
info:
  contact: {}
paths:
  /badges:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.Badge'
            type: array
      summary: GET Badges
  /health:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: Health check route
swagger: "2.0"
